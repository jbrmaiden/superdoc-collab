# Server Configuration
PORT=3050
NODE_ENV=development

# PostgreSQL Database
# For local development, use a local PostgreSQL instance
# For Render, this will be set automatically from the database connection
DATABASE_URL=postgresql://user:password@localhost:5432/superdoc

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173
