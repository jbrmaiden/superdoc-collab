{
  "name": "superdoc-collab",
  "version": "1.0.0",
  "private": true,
  "description": "SuperDoc collaborative document editor with PostgreSQL persistence",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\" --names \"server,client\" --prefix-colors \"blue,green\"",
    "build:server": "npm run build --prefix server",
    "build:client": "npm run build --prefix client",
    "build": "npm run build:server && npm run build:client",
    "postinstall": "npm run install:all"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
